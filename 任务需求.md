langchain v1.x核心组件地址
Models：https://docs.langchain.com/oss/python/langchain/models
Messages：https://docs.langchain.com/oss/python/langchain/messages
Tools：https://docs.langchain.com/oss/python/langchain/tools
Short-term memory：https://docs.langchain.com/oss/python/langchain/short-term-memory
Event streaming：https://docs.langchain.com/oss/python/langchain/event-streaming
Streaming：https://docs.langchain.com/oss/python/langchain/streaming
Structured output：https://docs.langchain.com/oss/python/langchain/structured-output


可以。你这个需求本质上不是“简单整理文档”，而是要让模型**按照 `任务需求.md` 中列出的 LangChain 官方文档路径，逐个解析核心组件，并把每个组件落成一个结构完整、可运行、可学习的 Python 项目**。

我建议把提示词强化成“**任务目标 + 输入约束 + 项目结构 + 内容要求 + 完整性要求 + 代码要求 + 输出要求 + 执行规则**”，这样后面让 AI Agent / Coding Agent 执行时，稳定性会高很多。

# LangChain 最新官方核心组件项目化学习项目

## 一、任务背景

当前项目中存在一个 `任务需求.md` 文件，该文件记录了 **LangChain 最新官方文档中各个核心组件的官方文档路径**。

项目中已经提前创建好了各个核心组件对应的文件夹。

现在需要你基于 `任务需求.md` 中记录的官方文档路径，对 LangChain 最新官方文档进行系统性学习、整理，并将每一个核心组件最终落地为一个**独立、完整、可运行、适合学习和实战的 Python 项目**。

注意：

这不是简单地把官方文档翻译成中文，也不是简单整理 API。

你的目标是：

> **以 LangChain 最新官方文档为主要依据，将每一个核心组件拆解成完整知识体系，并通过多个 Python 示例文件，把官方文档中的核心知识点、使用方式、底层概念、常见场景和最佳实践全部项目化。**

---

# 二、核心任务

首先读取项目根目录下的：

```text
任务需求.md
```

解析其中列出的所有 LangChain 官方文档路径。

然后按照文档路径对应的核心组件，逐个处理。

例如：

```text
任务需求.md
│
├── LangChain Models
├── Messages
├── Prompt Templates
├── Output Parsers
├── Structured Output
├── Tools
├── Tool Calling
├── Retrieval
├── Runnables
├── Agents
├── Memory
├── Middleware
├── LangGraph
└── ...
```

实际组件以 `任务需求.md` 中的内容为准，不允许自行猜测任务范围。

项目中已经存在对应的文件夹，你需要**直接在已有文件夹中创建和整理 Python 文件，不要重新创建一套重复的目录结构。**

---

# 三、每个核心组件的处理方式

对于 `任务需求.md` 中的每一个核心组件，都必须完成以下工作：

## 1. 阅读对应官方文档

首先访问并阅读对应的 LangChain 官方文档。

官方文档是第一信息来源。

需要重点提取：

* 核心概念
* API
* 架构
* 使用方式
* 参数
* 类
* 方法
* 工作机制
* 示例代码
* 官方推荐实践
* 注意事项
* 限制
* 常见组合方式
* 与其他 LangChain 组件的关系
* 最新版本中的变化

必须以**当前最新 LangChain 官方文档**为准。

不要默认使用旧版本 LangChain 的知识。

尤其注意 LangChain 版本迭代造成的 API 变化。

---

# 四、官方文档不完整时必须补全

官方文档中的示例可能存在以下问题：

* 只展示最简单用法
* 示例不完整
* 缺少底层原理
* 缺少参数解释
* 缺少异常情况
* 缺少实际业务场景
* 缺少不同 API 的对比
* 缺少与其他组件的组合方式
* 缺少完整项目案例

因此不能机械照搬官方文档。

你需要结合 LangChain 官方源码、API Reference、官方示例以及合理的工程实践，对知识体系进行补全。

但是需要严格区分：

### A. 官方明确提供的内容

标记为：

```text
官方文档
```

### B. 根据源码、API 行为推导出的内容

标记为：

```text
源码分析
```

### C. 工程实践中的扩展内容

标记为：

```text
工程实践
```

不要把自己推导的内容伪装成官方结论。

---

# 五、Python 文件拆分要求

每个核心组件不能只创建一个 Python 文件。

需要根据知识点进行合理拆分。

例如一个核心组件：

```text
01_models/
```

可以设计成：

```text
01_models/
├── 01_basic_chat_model.py
├── 02_model_invoke.py
├── 03_model_stream.py
├── 04_model_batch.py
├── 05_model_async.py
├── 06_model_config.py
├── 07_model_structured_output.py
├── 08_model_tool_calling.py
└── README.md
```

具体文件数量不要固定。

**一个 Python 文件原则上只负责讲清楚一个核心知识点。**

不要为了减少文件数量，把大量不同知识点全部塞进一个文件。

也不要为了增加文件数量，将一个简单概念机械拆成多个文件。

最终以：

> 一个文件 = 一个明确的知识点 / 一组强相关知识点

为原则。

---

# 六、每个 Python 文件必须“可学习”

每一个 Python 文件都不是简单 Demo。

每个文件必须做到：

1. 可以独立阅读
2. 可以理解当前知识点
3. 尽可能可以独立运行
4. 代码完整
5. 有清晰注释
6. 有必要的输入输出示例
7. 能体现当前知识点与其他概念的关系

每个 Python 文件建议包含以下结构：

```python
"""
知识点：
XXX

官方文档：
XXX

本示例主要学习：
1. XXX
2. XXX
3. XXX
"""

# ==============================
# 1. 环境准备
# ==============================

# ==============================
# 2. 核心代码
# ==============================

# ==============================
# 3. 运行示例
# ==============================

# ==============================
# 4. 结果说明
# ==============================

# ==============================
# 5. 知识点总结
# ==============================
```

根据具体场景灵活调整，不要求所有文件严格机械使用相同模板。

---

# 七、代码要求

所有代码必须尽可能基于**最新 LangChain API**。

禁止为了兼容旧代码而优先使用过时 API。

例如：

* 优先使用官方当前推荐 API
* 优先使用 Chat Model，而不是旧式 Completion LLM
* 优先使用当前推荐的 Runnable / Agent / Tool Calling 等机制
* 不要使用已经废弃的 API，除非当前文档明确要求学习它
* 如果新旧 API 都存在，必须明确说明区别

代码必须符合现代 Python 项目实践。

优先保证：

```text
可读性
正确性
可运行性
可维护性
```

不要写为了展示技巧而过度复杂的代码。

---

# 八、环境变量与模型调用

涉及 LLM 的示例时，不要把 API Key 硬编码在 Python 文件中。

统一使用：

```text
.env
```

例如：

```env
OPENAI_API_KEY=
DASHSCOPE_API_KEY=
```

Python 中使用：

```python
from dotenv import load_dotenv

load_dotenv()
```

如果不同示例需要不同模型，可以明确说明。

例如：

```text
MODEL_NAME=qwen3.5-plus
```

或者：

```python
model = init_chat_model(...)
```

具体模型以当前官方文档和项目实际环境为准。

---

# 九、第三方依赖要求

如果某个知识点需要额外安装依赖，必须明确说明。

例如：

```text
pip install langchain
pip install langchain-openai
pip install langchain-community
```

如果项目使用 `uv`，优先给出：

```text
uv add xxx
```

同时说明为什么需要这个依赖。

不要让每个 Python 文件都重复安装依赖。

应该在对应核心组件的 README 中统一说明依赖。

---

# 十、README 要求

每一个核心组件文件夹都必须有一个：

```text
README.md
```

README 必须包含：

## 1. 组件介绍

说明：

* 这个组件是什么
* 为什么 LangChain 要设计它
* 主要解决什么问题

## 2. 核心概念

系统介绍这个组件涉及的核心知识。

## 3. 知识点目录

列出当前目录中所有 Python 文件：

```text
01_xxx.py
02_xxx.py
03_xxx.py
...
```

并说明每个文件学习什么。

## 4. 学习顺序

给出推荐学习路径。

例如：

```text
基础概念
↓
核心 API
↓
进阶能力
↓
与其他组件结合
↓
实战
```

## 5. 常见问题

整理这个组件常见的坑。

## 6. 与其他 LangChain 组件的关系

例如：

```text
Model
   ↓
Prompt
   ↓
Runnable
   ↓
Tool
   ↓
Agent
```

根据实际组件进行说明。

---

# 十一、知识点完整性要求

这是本任务最重要的要求之一。

不能只覆盖官方文档标题。

需要对每一个核心组件进行**知识点完整性检查**。

至少从以下维度进行检查：

```text
核心概念
API
参数
生命周期
调用方式
同步
异步
Streaming
Batch
Callbacks
错误处理
配置
组合方式
与其他组件结合
底层机制
源码结构
常见问题
最佳实践
实际业务场景
版本变化
```

不是所有维度都适用于所有组件。

应根据组件类型合理选择。

最终目标是：

> 学完当前目录后，即使脱离官方文档，也能够真正理解这个 LangChain 核心组件，并能够独立使用它进行开发。

---

# 十二、理论与代码必须对应

禁止出现：

```text
讲了一大堆理论
但是代码完全没有体现
```

也禁止：

```text
只有代码
没有解释为什么这么写
```

必须尽量做到：

```text
知识点
↓
代码
↓
运行结果
↓
原理解释
↓
实际应用
```

形成完整学习闭环。

---

# 十三、增加“对比型示例”

对于容易混淆的知识点，需要专门增加对比代码。

例如：

```text
LLM vs Chat Model

invoke vs stream

invoke vs batch

RunnableLambda vs RunnablePassthrough

Tool Calling vs Agent

Memory vs State

Retriever vs VectorStore

Function Calling vs MCP
```

具体对比内容根据当前官方文档确定。

每个对比示例都需要明确说明：

```text
是什么
区别
使用场景
什么时候应该选择 A
什么时候应该选择 B
```

---

# 十四、增加“实战型示例”

每个核心组件在基础 API 示例之外，尽可能增加至少一个真实场景示例。

例如：

```text
Prompt
→ 构建问答 Prompt

Structured Output
→ 将用户输入转换成结构化 JSON

Tool
→ 构建天气查询工具

Retriever
→ 实现知识库检索

Agent
→ 实现多工具 Agent

Memory / State
→ 实现多轮对话
```

实战示例应当尽量贴近真实 AI 应用开发，而不是纯粹为了展示 API。

---

# 十五、项目最终结构

最终项目结构应该类似：

```text
项目根目录/
│
├── 任务需求.md
│
├── 01_xxx/
│   ├── README.md
│   ├── 01_xxx.py
│   ├── 02_xxx.py
│   ├── 03_xxx.py
│   └── ...
│
├── 02_xxx/
│   ├── README.md
│   ├── 01_xxx.py
│   ├── 02_xxx.py
│   └── ...
│
├── 03_xxx/
│   ├── README.md
│   ├── 01_xxx.py
│   └── ...
│
└── ...
```

**不要擅自删除用户已经创建好的文件夹。**

如果目录已经存在 README，也应该在原有基础上完善，而不是随意覆盖已有内容。

---

# 十六、命名规范

Python 文件采用：

```text
01_xxx.py
02_xxx.py
03_xxx.py
```

形式。

名称必须能够直接看出当前文件讲什么。

例如：

```text
01_basic_model.py
02_model_invoke.py
03_model_stream.py
04_model_batch.py
```

禁止：

```text
test1.py
demo.py
test.py
example.py
aaa.py
```

这种没有明确语义的命名。

---

# 十七、代码注释要求

注释重点解释：

```text
为什么这么写
这个 API 做什么
这个参数有什么作用
底层发生了什么
和其他 API 有什么区别
```

不要大量写：

```python
# 定义变量
x = 1
```

这种没有学习价值的注释。

---

# 十八、运行结果要求

对于关键示例，需要展示预期运行结果。

例如：

```text
运行：

python 02_model_invoke.py

输出：

AIMessage(...)
```

如果输出具有动态性，可以说明：

```text
实际模型输出会根据模型和输入变化，以下仅展示典型结果。
```

不能伪造没有实际运行过的结果。

---

# 十九、异常与边界情况

对于重要 API，需要适当覆盖异常情况。

例如：

```text
参数错误
API Key 缺失
模型不存在
Tool 参数不符合 Schema
Structured Output 解析失败
Retriever 没有检索结果
Agent 工具调用失败
```

不要只展示 Happy Path。

---

# 二十、版本兼容与时效性

由于 LangChain 更新非常快，所以：

**必须以当前最新官方文档为准。**

如果发现：

```text
旧教程写法
≠
当前官方推荐写法
```

必须采用当前官方写法。

同时在 README 或对应 Python 文件中说明：

```text
旧版本写法：
XXX

当前推荐写法：
XXX
```

如果某个 API 已经 deprecated，需要明确标记：

```text
⚠️ Deprecated
```

并说明替代方案。

---

# 二十一、不要过度依赖官方示例

官方示例只是基础参考。

最终成果应该是：

```text
官方文档
+
源码理解
+
API Reference
+
知识体系梳理
+
工程实践
+
完整代码示例
```

而不是：

```text
官方文档复制粘贴
```

---

# 二十二、执行顺序

按照 `任务需求.md` 中的顺序逐个处理。

每完成一个核心组件，需要完成：

```text
1. 阅读官方文档
2. 梳理知识体系
3. 确定知识点列表
4. 创建 / 整理 Python 文件
5. 编写示例代码
6. 编写 README
7. 检查 API 是否为最新版本
8. 检查代码完整性
9. 尽可能运行并验证代码
10. 修复发现的问题
```

然后再继续处理下一个核心组件。

---

# 二十三、最终质量标准

最终项目不是“代码仓库”，而应该成为一套：

> **LangChain 最新版本核心知识体系 + 官方文档学习笔记 + 可运行 Python Demo + AI Agent 工程实践的完整学习项目。**

最终需要达到以下效果：

```text
看 README
    ↓
知道当前组件是什么
    ↓
知道核心概念
    ↓
知道 API
    ↓
运行 Python 示例
    ↓
理解 API 行为
    ↓
理解底层机制
    ↓
知道与其他组件如何组合
    ↓
知道真实项目什么时候使用
```

---

# 二十四、特别要求

请严格遵守以下原则：

1. **不要遗漏 `任务需求.md` 中的核心组件。**
2. **不要只复制官方示例。**
3. **不要使用过时 LangChain API 作为主要实现。**
4. **不要把所有内容塞进一个 Python 文件。**
5. **不要为了拆文件而机械拆分。**
6. **不要生成没有实际学习价值的 Demo。**
7. **不要硬编码 API Key。**
8. **不要伪造运行结果。**
9. **不要擅自删除已有目录和已有有效代码。**
10. **发现官方文档内容不足时，需要主动补全知识体系。**
11. **发现 API 存在版本差异时，需要明确说明。**
12. **代码必须围绕知识点组织，而不是围绕 API 名称机械组织。**
13. **优先保证内容准确、完整、可运行。**
14. **所有核心组件最终都应该形成独立、完整的学习单元。**

---

# 二十五、最终验收标准

完成后，请对整个项目进行一次整体检查：

### 文档层面

检查：

```text
任务需求.md 中的每个组件
是否都已经处理
```

### 文件层面

检查：

```text
每个组件是否都有 README.md
是否都有合理数量的 Python 文件
文件命名是否规范
```

### 知识层面

检查：

```text
核心概念是否完整
API 是否覆盖
常见场景是否覆盖
边界情况是否覆盖
组件之间的关系是否解释
```

### 代码层面

检查：

```text
是否使用最新 API
是否存在废弃 API
是否存在明显 Bug
是否存在硬编码 Key
是否可以正常运行
```

### 学习体验层面

检查：

```text
初学者能否从 README 开始学习
每个 Python 文件是否只关注明确的知识点
代码和理论是否对应
是否能够从基础逐渐学习到进阶
```

---

# 最终目标

最终请将这个项目打造为一套真正可以长期维护的：

**《LangChain 最新版本核心组件源码级学习与实战项目》**

而不是简单的：

**《LangChain 官方文档代码搬运项目》**。

请始终围绕以下四个目标进行实现：

> **完整性**
>
> **准确性**
>
> **可运行性**
>
> **可学习性**
